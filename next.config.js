/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // Optional, remove if you are not using App Directory
  },
};

module.exports = nextConfig;